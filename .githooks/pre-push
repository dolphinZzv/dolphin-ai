#!/bin/bash
set -euo pipefail

echo "=== Building ==="
go build ./...

echo "=== Running tests ==="
pkgs=$(go list ./... | grep -v "internal/limit")
go test $pkgs -count=1

echo "=== All checks passed ==="
