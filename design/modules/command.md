# User Commands (`internal/command/` — v0.3)

类似 Skills，但通过 `/command_name` 在对话中由用户触发。

- 多目录加载: `~/.dolphin/commands/` + `.dolphin/commands/`
- YAML frontmatter + Markdown body
- 使用跟踪 (call_count, last_called_at)

<!-- last-modified: 2026-05-13 -->
