<Window x:Class="Dolphin.WebHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dolphin WebHost"
        Width="480"
        Height="320"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="16"
                   FontWeight="Bold"
                   Text="Dolphin WebHost"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   x:Name="StatusText"
                   Text="Starting..."
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   Margin="0,0,0,8"/>

        <ListBox Grid.Row="2"
                 x:Name="SessionList"
                 Margin="0,0,0,8">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock FontWeight="Bold"
                                   Text="{Binding SessionId}"/>
                        <TextBlock Text="{Binding Url}"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="ShowWindowBtn"
                    Content="Show Window"
                    Margin="0,0,8,0"
                    Padding="8,4"
                    Click="OnShowWindowClick"/>
            <Button x:Name="ExitBtn"
                    Content="Exit"
                    Padding="8,4"
                    Click="OnExitClick"/>
        </StackPanel>
    </Grid>
</Window>
