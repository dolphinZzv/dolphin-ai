# deploy

Deploy the application to production.

## Usage
When user says "deploy" or "部署", run the deployment script.

## Instructions
Execute `/root/dolphin/scripts/deploy.sh` if it exists, otherwise list the current git status and latest commit.
